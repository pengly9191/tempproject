#pragma once

#include "head.h"
/*
class ImageProcess
{
public:

    int Process(int id,Mat &gray);

private:
    int image_read(int id);
    int image_rotate();

private:
    Mat gray_;
    int id_;
};
*/
